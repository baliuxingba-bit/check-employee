// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model TrackedItem {
  id               String    @id @default(cuid())
  category         String
  subject          String
  detail           String
  expiryDate       DateTime?
  notificationDate DateTime?
  notifiedAt       DateTime?
  notes            String?
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt
}

model AbsenceRecord {
  id           String   @id @default(cuid())
  employeeName String
  date         DateTime
  type         String
  notes        String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model Employee {
  id          String    @id @default(cuid())
  name        String    @unique
  affiliation String?
  joinDate    DateTime?
  birthDate   DateTime?
  createdAt   DateTime  @default(now())
}

model Category {
  id        String   @id @default(cuid())
  name      String   @unique
  createdAt DateTime @default(now())
}

// Note: ReportField / ReportRecipient / DailyReport tables still exist in this
// database (created by an earlier migration here) but are now owned and used
// by the separate "daily-report" app. Intentionally not declared in this
// project's schema anymore — do not add a migration that drops them.
